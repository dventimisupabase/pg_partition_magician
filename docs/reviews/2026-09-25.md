# Review pass 2: 2026-09-25

pinned: `581e87a` (v0.6.0 plus 30 commits (all 25 pass 1 fixes and the method tooling)) | budget: 10 finders (8 fable/high, 2 opus on duplicated slices), 19 to 35 min each; 49 verifiers (opus/medium); 4662879 agent tokens
lenses: fresh surface, concurrency and locks, time and zones, upgrade and install paths, identity and names, tests that pass for the wrong reason, contracts versus documentation | previous pass lenses: none declared (pass 1 reviewed by module slice)
seeds K=9, recall 1.00; claims 73; findings 49; precision 1.00
findings by tier: T1 7 T2 24 T3 13 T4 2 T5 3
cost per finding: 95160.8; per Tier 1 finding: 666125.6
root causes: 49 distinct verifier root-cause statements behind the findings; grouped into 27 classes below; closed as a class: to be filled after the fix phase
known and open (re-found, unfixed from earlier passes): 0
capture-recapture (T1): not attempted
blind spots (seeds missed, by lens): none

Per finder (claims, precision): F1 (8, 1.00), F10 (10, 1.00), F2 (6, 1.00), F3 (5, 1.00), F4 (6, 1.00), F5 (7, 1.00), F6 (5, 1.00), F7 (10, 1.00), F8 (6, 1.00), F9 (10, 1.00)
Seed hits 24, candidates 49, fell 0, unverified 0, hypotheses 0.

## Findings

| tier | finding | issue | fix PR |
|---|---|---|---|
| 1 | F1-08: The cutover replays the original table's triggers onto the new parent from pg\_get\_triggerdef, which does not carry tgenabled, so every trigger comes back ENABLED ('O'): a trigger the operator had DISABLED fires again on the next write and silently rewrites the stored row (n = 42 where 0 was stored before conversion), and an ENABLE ALWAYS / ENABLE REPLICA trigger silently stops or starts firing under session\_replication\_role = replica; nothing is refused or logged, where reference.md says the parent takes over the table's row triggers. | #499 | |
| 1 | F10-07: native bounds are stored as timestamptz::text in the writing session's DateStyle; after transmute under DateStyle 'SQL, DMY' ('01/10/2026' = 1 Oct), a maintain tick under the default 'ISO, MDY' reads every hi as January of its year, so retain()/retire() drop live partitions (all rows of a time table, today's included; the June 2020 row of a uuidv7 table); the stored bounds should mean the same instant in every session | #500 | |
| 1 | F5-03: The cutover records pg\_get\_constraintdef() of each incoming FK, which names the referenced table unqualified whenever it is visible on the transmuting session's search\_path; restore\_incoming\_fks replays that text in a later session (pg\_cron's default search\_path), so the key is re-added against whatever 'orders' resolves to there (an unrelated public.orders, logged restore\_incoming\_fk) or fails every tick when nothing does, where the key should come back against the parent it was dropped from regardless of session. | #498 | |
| 1 | F6-02: archive.\_encode\_upload\_ndjson\_single (lines 2290-2296) and archive.\_encode\_upload\_parquet (2351-2354) call pgpm.\_encode without config.partition\_tz, so the chunk's [lo, hi) is rendered as UTC wall time; on a naive timestamp control column whose grid is recorded in another zone (#455) the strategy reads a range shifted by the zone offset, uploads the wrong rows, and still returns covered\_hi = p\_hi, so retire() drops the child with its own rows never archived; it should encode in partition\_tz exactly as pgpm.\_next\_archive\_chunk and pgpm.\_archive\_noop do. | #501 | |
| 1 | F6-03: The archive\_fn transports name the uploaded object from regexp\_replace(p\_lo, '[^0-9]', '', 'g') (also line 2368 for Parquet), which drops the sign, so chunk lo = -10000 and chunk lo = 10000 of the same table share one S3 key; one maintain() tick uploads the second over the first, both ledger rows record the same s3\_key as archived, and the [-10000, 0) partition's rows are gone from S3 while retire() will drop it; two different chunks must map to two different keys. | #502 | |
| 1 | F9-03: after ALTER TABLE ev RENAME TO events mid-regrain, the source's capture trigger still writes ev\_pgpm\_regrain\_delta (name baked in at prepare) while \_regrain\_capture\_names resolves the delta from the parent's current name, so reconcile, the swap gate and the swap see no delta and swap: a committed UPDATE reverts, a DELETE comes back and an INSERT vanishes; the captured changes should be honoured (or the regrain refused) regardless of the parent's name | #496 | |
| 1 | F9-04: a writer's captured UPDATE takes a pgpm\_seq below the reconcile's watermark but is uncommitted while the reconcile applies its batch and commits before the reconcile's final 'delete from delta where pgpm\_seq <= wm' (a later READ COMMITTED snapshot), so its delta rows are deleted unapplied and the swap attaches the stale fine child: the committed UPDATE of id 150000 reverts to 'orig'; only delta rows the apply statements actually saw should be consumed | #497 | |
| 2 | F1-04: With a date control column, a day (fixed-seconds) step and a non-UTC session zone, \_time\_literal renders the 00:00Z lattice boundary as the previous local date at 20:00 with an offset, which a date column truncates to the previous date; the monolith bound becomes `d < today` while the grid believes it covers today, phase 2's VALIDATE fails on today's rows with a raw 23514 after phase 1 committed, and the NOT VALID CHECK is left rejecting every insert dated today; the docs say a date column is supported (read as wall time in the zone) and that refusals leave the table untouched. | #504 | |
| 2 | F1-05: \_part\_name lets <rel>\_p<label> silently truncate to 63 bytes ('cosmetic, never a correctness concern'), but obtain tests candidate existence by name, so for a relation name of ~58+ characters the monolith takes the truncated name and every forward candidate is skipped as already existing: transmute completes with no forward grid, no tick ever adds one, nothing is logged, and the first write past the monolith's hi fails with 'no partition of relation found for row'. | #510 | |
| 2 | F1-06: With a timestamp-without-time-zone control column, an hourly step and a DST-observing partition\_tz, the two hourly cells either side of the autumn fall-back render to the same naive wall time (05:00Z and 06:00Z are both 01:00:00 in America/New\_York), so the CREATE TABLE ... FOR VALUES FROM ('01:00:00') TO ('01:00:00') fails with 'empty range bound'; obtain fails every tick once the lookahead reaches that hour and extend\_to fails the same way, so the grid can never extend past the fall-back and writes fail when the frontier reaches the ceiling; transmute accepts the shape without refusing. | #504 | |
| 2 | F1-07: \_transmute never checks that p\_parent is a plain table or not already in pgpm.config, so re-running transmute on a converted table (the documented remedy after any failure) adds pgpm\_monolith\_bound NOT VALID to the live partitioned parent (propagating to the forward partitions), commits, validates it, and only then fails in the cutover on the monolith's name; the bound stays on the parent rejecting every write outside the original monolith's range, i.e. every current write, until abort or reap, and this session's abort is refused. | #509 | |
| 2 | F10-02: \_next\_archive\_chunk probes the chunk boundary with max(<control>) (and min(<control>) at line 2102), which does not exist for uuid, so on a uuidv7 table with an archive\_fn every maintain() tick logs skip\_archive 'function max(uuid) does not exist', no ledger row is ever written and the aged partition is never retired; the archive step should size the chunk (e.g. ORDER BY ... LIMIT 1 as \_frontier\_native does) and retention should proceed | #507 | |
| 2 | F10-03: for text\_time (and uuidv7) the 'next distinct control value' that ends a chunk is decoded to the native grid at the encoding's unit, so when one unit (a second for ObjectId/KSUID, a ms for uuidv7/cuid) holds a chunk's worth of rows v\_stop == v\_lo, \_next\_archive\_chunk returns nothing, \_archive\_step continues without logging, and the partition is never archived further nor retired; the chunk should extend to the next native value (documented: 'extended to the next distinct control value so a run of ties never splits') and archiving should progress | #513 | |
| 2 | F10-05: untransmute on a monolith that retention has write-blocked (archiving deferred) detaches it and drops the parent but leaves the pgpm\_write\_block trigger on it, so the restored, unmanaged table rejects every write ('past its retention boundary') with no pgpm state left to lift it; untransmute should remove pgpm's block (or refuse) rather than return a read-only table | #508 | |
| 2 | F10-06: after regraining a partly archived, retention-aged coarse child (allowed since #278), the source's pgpm.archive\_ledger rows survive the swap keyed (parent\_table, lo), so the first fine child's first chunk insert collides on archive\_ledger\_pkey, \_archive\_step raises every tick (skip\_archive 'duplicate key'), and no partition of the parent is ever archived or retired again; the swap should retire the source's ledger rows (or the ledger key should not collide) and the fine children should archive and drop | #511 | |
| 2 | F10-08: guide.md says to rename a partition by updating pgpm.part.child\_name 'and nothing else'; on a partly archived partition that orphans its pgpm.archive\_ledger rows under the old name, archiving restarts from lo and its first ledger insert collides on archive\_ledger\_pkey every tick (skip\_archive), so the partition is never archived or retired; the documented procedure should keep coverage attached (or the ledger should not be keyed on the name) | #511 | |
| 2 | F10-10: \_is\_write\_blocked casts the raw schema name with v\_nsp::regnamespace, which parses it as an identifier, so for a managed table in a schema needing quotes ("Sales") the archive step raises 'schema "sales" does not exist' every tick (skip\_archive), nothing is archived and the aged partition is never retired, while the same table in a lower-case schema archives and drops; the lookup should match pg\_namespace.nspname (same pattern at line 2470 in \_regrain\_capture\_active, which logs skip\_regrain\_capture every tick) | #512 | |
| 2 | F2-03: In a partition\_tz where midnight on the 1st falls in a DST gap (America/Asuncion 2017-10 and 2023-10, Asia/Amman 2016-04), \_grid\_floor's month branch resolves the non-existent wall time an hour later and \_grid\_next then reads the wall clock back off that shifted instant, so next(floor(Oct)) = 01:00 Nov 1 while floor(Nov) = 00:00 Nov 1; regrain\_step walks with that pair, the October child ends an hour after the November child begins, and the swap fails with 'would overlap' (skip\_regrain on every auto-regrain tick, forever), where the two functions should describe one lattice and the history should regrain. | #505 | |
| 2 | F2-04: regrain\_step's copy microbatch seeds its watermark with `select max(d2.<control>) from <fine child>`; PostgreSQL has no max(uuid), so every regrain of a uuidv7 table fails at its first copy with 'function max(uuid) does not exist' (synchronously via pgpm.regrain/regrain\_history, and as a skip\_regrain row on every maintain tick once set\_regrain has armed auto-regrain), so a uuidv7 history can never be split; \_frontier\_native already avoids max() for exactly this reason. | #507 | |
| 2 | F2-05: set\_partition\_tz accepts any zone on a day-denominated grid (comment and docs/reference.md: 'an absolute lattice in every zone, so its zone can always change (only the names move)'), but \_part\_name renders a day label as the wall date of the cell's lo in partition\_tz, so after a change to a zone west of the old one every cell's new label is the old label of the cell before it; obtain()'s `to\_regclass(name) is not null` check (line 1061) then skips the first cell past the existing grid because its new name already belongs to the last existing child, creates the cell after it, and repeats the skip on every tick, leaving a permanent one-day hole in which every write is refused once the frontier reaches it. | #503 | |
| 2 | F2-06: \_part\_name renders day and week labels as the wall date of the cell's lo in partition\_tz; in Atlantic/Azores (winter -01, summer +00) the 00:00Z lattice cell starting on the autumn fall-back Sunday and the cell starting the next day render the same date, so obtain()/extend\_to()'s `to\_regclass(name) is not null` check (lines 1061 and 1146) skips the second cell as already existing on every tick, leaving a permanent one-day hole every autumn in which every write is refused; the same collision was recognised and avoided for hour/minute labels by rendering them in UTC (#455) but not for day labels, and the same mechanism opens the hole after set\_partition\_tz to a westward zone (F2-05). | #503 | |
| 2 | F3-03: maintain() re-applies set\_config('lock\_timeout','200ms',true) after its first two COMMITs but not after the retain COMMIT that precedes the auto-regrain block, so regrain\_step (and its swap's DETACH, which takes ACCESS EXCLUSIVE on the parent) runs under the session default lock\_timeout = 0: with a writer holding one row in the source, maintain waited the writer's full 5 s and swapped inside the tick, with every query on the parent queued behind the pending ACCESS EXCLUSIVE meanwhile, where docs/reference.md (regrain\_step) promises 'Under maintain the DETACH gives up after 200 ms (a skip\_regrain row, retried next tick)'. | #514 | |
| 2 | F4-03: untransmute() returns the monolith as the restored table with pgpm's pgpm\_write\_block BEFORE ROW trigger (ENABLE ALWAYS) still on it, so every INSERT/UPDATE/DELETE on the restored table fails with 'past its retention boundary and is no longer writable' and, with pgpm.config/pgpm.part cleared, no tick can ever lift it; the trigger capture only reads the PARENT's pg\_trigger, and the block sits on the child. Expected: a metadata-only reverse that undoes what transmute/maintenance put on the table (docs/reference.md 'untransmute'). | #508 | |
| 2 | F8-03: set\_partition\_tz accepts a zone change for a naive timestamp/date control column on a day step (the lattice check passes), but a naive column's bound literals are rendered as wall time in partition\_tz, so obtain then creates partitions whose catalog bounds are shifted by the zone offset against every existing one: pgpm.part disagrees with pg\_class and a permanent wall-clock hole opens in which inserts fail with no partition found; it should refuse the change for a naive column or render consistently. | #504 | |
| 2 | F8-04: \_part\_name renders a day-step label as the wall DATE in partition\_tz although the day grid is a fixed 86400 s lattice from the anchor instant, so in a DST-observing zone whose anchor is local midnight in the DST season (e.g. Australia/Sydney, anchor midnight 1 Jan) the two cells straddling the autumn fall-back share a name; obtain/extend\_to skip a candidate whose name exists and leave a permanent one-day hole into which inserts fail; the label must be unique per cell (render in UTC as hour labels do, or refuse the anchor). | #503 | |
| 2 | F8-05: set\_partition\_tz accepts a day-step zone change on a timestamptz column ('only the names move'), but obtain skips a candidate whose name already exists and the shifted labels move each cell's name onto its neighbour's, so the first cell past the existing grid is never created: a permanent one-day hole in which inserts fail with no partition found; obtain should test coverage, not the label, or set\_partition\_tz should refuse a shift that renames onto an existing cell. | #503 | |
| 2 | F8-06: a transmute resumed from a session in a different TimeZone reuses the phase-1 bound (computed on the first session's lattice, #275) but records config.partition\_tz from the resuming session (#455) because pgpm.transmute\_inflight does not carry the zone, so obtain's candidates half-overlap the monolith and a hole one full step wide is left past its hi (writes there fail) and set\_partition\_tz refuses the repair; the zone should be recorded with the claim and reused, or the resume refused. | #506 | |
| 2 | F9-06: the capture trigger runs as the writer (no SECURITY DEFINER) and inserts into <parent>\_pgpm\_regrain\_delta, which is created by the maintenance role with no grants, so an application role holding full DML grants on the parent gets 'permission denied for table pv\_pgpm\_regrain\_delta' on every UPDATE/DELETE/INSERT into the regraining child for the whole regrain; writes the parent's grants permit should keep working (the delta needs the parent's writers granted, or equivalent) | #496 | |
| 2 | F9-08: regrain\_step's copy resumes from coalesce((select max(d2.<control>) from <fine child>), lo), and PostgreSQL has no max(uuid), so on a uuidv7 table every copy tick raises 'function max(uuid) does not exist': regrain() of a frozen uuidv7 monolith fails and auto-regrain logs skip\_regrain every tick, so a uuidv7 monolith can never be regrained; the resume point should be read with ORDER BY <control> DESC LIMIT 1 as \_frontier\_native does | #507 | |
| 2 | F9-09: with partition\_tz America/New\_York, a '1 day' step and the anchor at a local (EDT) midnight, the absolute 86400 s cells starting 00:00 EDT and 23:00 EST on the fall-back Sunday both render \_p<date of that Sunday>, obtain skips the second as existing, and [Sun 23:00 EST, Mon 23:00 EST) is never built: writes on that Monday fail 'no partition of relation found for row'; day labels must be unique per cell (as the UTC rendering already guarantees for sub-day steps). Outside slice S3 proper; regrain's fine-child naming inherits it for a daily target. | #503 | |
| 2 | F9-10: set\_regrain compares the target with partition\_step only at the anchor, so '30 days' on a '1 month' grid is accepted as finer, but a 30-day cell starting in February is wider than a calendar month, so maintain's oldest-coarse-child candidate query picks it every tick, regrain\_step answers nosubdiv, and every coarse child behind it (here the second 90-day child) is never auto-regrained; set\_regrain should refuse a target wider than some month (or the candidate test should use the target), as #341 promises for coarser targets | #515 | |
| 3 | F1-02: After a cutover failure (e.g. lock\_timeout in phase 3) the transmute\_inflight claim is owned by the operator's still-connected session, and the take-over predicate `where not \_session\_alive(owner)` never fires for the owner itself, so the documented remedy (re-run transmute: 'resumes from the recorded bound') is refused with 'already in progress in another session' and transmute\_abort refuses too; the write-rejecting pgpm\_monolith\_bound CHECK stays on the table until that session disconnects, which no document says. | #509 | |
| 3 | F1-03: The up-front orphan guard only matches standalone tables named <rel>\_p<digits>(\_<digits>)* or <rel>\_p<19 digits>, so a relation already holding the monolith's own coarse name <rel>\_p<lo>\_to\_<hi> (or any non-table relation with a child name) is not refused; phases 1 and 2 commit and the cutover's RENAME fails with a raw 42P07, leaving the validated write-rejecting pgpm\_monolith\_bound CHECK and the transmute\_inflight claim on the table, where reference.md promises the collision is refused up front with the table untouched. | #509 | |
| 3 | F10-09: with auto-regrain, an archive\_fn and retain all on, retire() drops the coarse source of an in-flight regrain once archiving covers it (it refuses only unattached children), orphaning the regrain's standalone fine copies (unattached pgpm.part rows, real tables still holding rows 1..299 that retention dropped) and leaving regrain\_cursor set, with no later tick cleaning up; retire should refuse (or cancel the regrain) instead | #519 | |
| 3 | F3-02: \_regrain\_capture\_install reuses the persistent per-parent delta table whenever it exists, with the key columns of the FIRST regrain, while recreating the trigger function from the CURRENT key; after a key column is renamed between two regrains (PK (id,k2) -> (id,k3)) the prepare tick succeeds and then every INSERT/UPDATE/DELETE on the source child raises 'column k3 of relation b\_pgpm\_regrain\_delta does not exist' for the life of the regrain, where the reference promises committed DML against the source is honoured; it should refuse at prepare or rebuild the delta to match the key. | #496 | |
| 3 | F3-05: set\_regrain(parent, null) under an in-flight auto-regrain clears regrain\_to but not regrain\_cursor: maintain stops calling regrain\_step (the run never completes, its copies stay on disk) and \_enforce\_regrain\_capture keeps capture on the child whose range covers the cursor (never swept), so after 200 maintain ticks the capture trigger, TRUNCATE guard and delta are still on the source, no regrain\_capture\_orphan row exists and TRUNCATE parent is refused as 'a regrain is in flight'; docs/reference.md (regrain\_cancel) promises maintain sweeps it and logs regrain\_capture\_orphan, and docs/runbook.md (Disk is filling during a regrain, step 2) tells the operator to type set\_regrain(null) because 'the in-flight one still completes'. | #516 | |
| 3 | F4-04: pgpm.set\_archive\_fn accepts any function whose argument list is (regclass, name, text, text) regardless of return type (the regprocedure cast checks arguments only, and no prorettype check follows), so a strategy declared `returns text` is installed; docs/reference.md 'Archive strategy contract' says the cast validates 'exactly this signature' where the contract is `returns pgpm.archive\_result`. At tick time \_run\_archive\_strategy maps the single column positionally onto covered\_hi, and a strategy echoing p\_hi passes the contract check, records coverage with nothing archived, and the partition is dropped. Expected: set\_archive\_fn refuses a function not returning pgpm.archive\_result. | #517 | |
| 3 | F4-05: untransmute() during an in-flight regrain raises 'cannot drop function <rel>\_pgpm\_regrain\_capture() because other objects depend on it': regrain\_step's pgpm\_regrain\_capture trigger is on the monolith child, which untransmute detaches and renames into the restored table with the trigger still attached, so its own `drop function` of the capture function fails; neither the documented one-way-door refusal nor the clean reverse the code comment intends ('Drop it here or untransmute leaves it orphaned') happens, and only regrain\_cancel() first (undocumented for this case) unblocks it. | #508 | |
| 3 | F4-06: \_enforce\_write\_blocks decides 'coverage found without its block' by NAME (\_is\_write\_blocked(child\_name)) before the #429 identity check later in the same loop body, so when another relation holds a partition's name the tick deletes the pgpm.archive\_ledger rows describing the REAL partition (still attached, still write-blocked, unchanged) and logs archive\_coverage\_reset, while also logging fail\_write\_block\_identity for the same child; once the name is sorted out archiving restarts from lo. Expected: the identity mismatch fails closed and the real partition's valid coverage is left alone (docs/reference.md 'maintain': coverage is discarded for a partition that has NO trigger). | #518 | |
| 3 | F5-02: With p\_incoming\_fks => 'preserve', a self-referential FK is recorded with referencing\_table = the original table's oid, which the cutover renames into the monolith child; restore\_incoming\_fks then re-adds the key on that one partition (logged restore\_incoming\_fk) so rows routed to any forward partition escape it and orphans are accepted, where guide.md promises self-referential FKs are preserved and reference.md that the key is re-added against the new parent. | #498 | |
| 3 | F5-07: pgpm.dropped\_fk anchors the referencing table by oid; when that table is transmuted before the key is restored, its cutover renames the oid into its monolith child and restore\_incoming\_fks re-adds the key on that single partition (logged restore\_incoming\_fk), so referencing rows routed to any forward partition escape the key, where the docs promise foreign keys in both directions survive and the key is re-added against the new parent (same anchor as F5-02, reached by converting the referencing table). | #498 | |
| 3 | F6-04: archive.to\_s3 never reads archive.config.compress: with compress = true it uploads plain NDJSON at <prefix><child>.ndjson, while pgpm\_archive/README.md says 'GZIP compression applies to either format (archive.config.compress ...)' and docs/reference.md says the synchronous functions take compression from archive.config; the export should be GZIP (as archive.to\_s3\_parquet and both archive\_fn transports are) or the docs should not promise it. | #520 | |
| 3 | F6-05: Both SigV4 signers (line 219 and line 287) stamp x-amz-date and the credential scope from now(), the transaction start time, so every S3 request a transaction makes past 15 minutes is refused by S3/MinIO with 403 RequestTimeTooSkewed; archive.to\_s3 holds one transaction for a whole multipart export (README: 'handles any size') and a maintain() tick holds one for every chunk of \_archive\_step, so a long export aborts and a long backlog tick logs skip\_archive for every later chunk; the signature must be stamped with clock\_timestamp(). | #520 | |
| 3 | F9-05: reference.md says abandoning a regrain by turning auto-regrain off mid-flight is safe because maintain sweeps orphaned capture each tick (so no regrain\_cancel is needed), but \_enforce\_regrain\_capture keeps capture on the child covering regrain\_cursor and set\_regrain(null) leaves the cursor set, so after set\_regrain(null) and further ticks the capture trigger and TRUNCATE guard stay installed, no regrain\_capture\_orphan is logged, the delta keeps growing and TRUNCATE stays refused; the doc should say regrain\_cancel is required (tests/69 only 'abandons' by nulling the cursor by hand) | #516 | |
| 4 | F5-05: The Install section's database.dev/TLE snippet tells the operator to type create extension ... version '0.4.0', two releases behind the 0.6.0 this tree installs (pgpm.version(), extension.control), so following the guide installs a release the CHANGELOG lists fixes for; the snippet should name the current version or omit the version clause. | #521 | |
| 4 | F5-06: reference.md's from\_hypertable Foreign keys section says the outgoing-key carry is 'Logged from\_hypertable\_carry\_fk and from\_hypertable\_adopt\_fk', but no routine in pgpm\_core or pgpm\_hypertable writes 'from\_hypertable\_adopt\_fk' (the cutover's adopt step was removed and transmute's re-add logs nothing), so an alert on that action never fires; the sentence should name only what is logged, since RELEASING.md makes log actions part of the contract. | #521 | |
| 5 | F7-02: throws\_ok($$ call pgpm.transmute('public.ev72t', ...) $$, NULL, '<description>') resolves to errcode NULL and errmsg NULL, so it accepts any error, including the 2D000 a transmute that did NOT refuse the transition-table trigger would raise at its first COMMIT inside the wrapper; the following relkind='r' check passes then too (the statement rolled back), so the refusal contract is untested; it should pin 'P0001' or the pgpm message. | #522 | |
| 5 | F7-03: Three timescale refusal assertions wrap a committing procedure in throws\_ok(..., NULL, NULL, desc) (10:55 and 14:133 around from\_hypertable\_copy's p\_track\_changes refusals, 08:23 around from\_hypertable\_cutover): a copy that did NOT refuse commits per chunk and dies with 2D000 inside the wrapper, which NULL/NULL also accepts, so the keyless and nullable-key refusals pass with the refusal removed; they should pin 'P0001' or the message. | #522 | |
| 5 | F7-04: perf.yml lists tests/77, 107 and 111 because bench wrappers drive them through the discriminate step, but the three identity wrappers (archive\_identity\_substitution.sh, retire\_identity\_unreferenced.sh, write\_block\_identity.sh) drive tests/102, 103 and 104, which are not listed: a PR that weakens one of those files runs the matrix (green on correct code) and no perf job, so its discrimination proof is never re-run, while scripts/check\_track\_filters.py reports PASS because it reads run\_perf's body rather than the wrappers discriminate.sh drives; the three files should be in the filter like the other three. | #522 | |

## Null results (by lens)

(from the finders' null-results files)

## Fell in verification

none

## Known and open

none

## Hypotheses (not counted)

none

## Stopping criteria status

This pass's half; the criteria need the previous pass as well.

- zero Tier 1 findings: NOT met
- seed recall >= 0.8: met
- precision >= 0.7: met

## Root causes

| group | tier | mechanism | findings |
|---|---|---|---|
| RC1 | T2 | Day-step partition names are the wall date in partition_tz while the day grid is a fixed 86400 s lattice: adjacent cells share a name and obtain/extend_to skip the second, leaving a permanent hole | F2-05, F2-06, F8-04, F8-05, F9-09 |
| RC2 | T2 | Naive (timestamp/date) control bounds are rendered as wall time with an offset in partition_tz: previous-date monolith bound, empty hourly range across DST, zone-change hole | F1-04, F1-06, F8-03 |
| RC3 | T2 | Month step across a DST gap at midnight: _grid_next(_grid_floor(Oct)) is not _grid_floor(Nov), regrain overlaps | F2-03 |
| RC4 | T2 | A transmute resumed from a differently zoned session keeps the first session's bound but records the second session's partition_tz | F8-06 |
| RC5 | T2 | max(uuid)/min(uuid) do not exist before PG18: uuidv7 regrain copy and archive chunk sizing fail every tick | F2-04, F9-08, F10-02 |
| RC6 | T1 | Regrain change capture: delta resolved by the parent's current relname (rename loses every captured change); delta table reused across key renames; no grants for app roles | F9-03, F3-02, F9-06 |
| RC7 | T1 | _regrain_reconcile deletes delta rows by watermark in a later snapshot than the apply statements used, consuming a committed change unapplied | F9-04 |
| RC8 | T2 | untransmute residue: pgpm_write_block left on the restored monolith; teardown drops the capture function while its trigger still rides the monolith | F4-03, F10-05, F4-05 |
| RC9 | T1 | dropped_fk records are not identity-anchored: definition text is search_path-relative; referencing_table oid moves onto the monolith when that table is transmuted | F5-03, F5-02, F5-07 |
| RC10 | T2 | transmute preconditions: no relkind/config check (re-run on a converted table); orphan guard misses the monolith's own coarse name; same-session retry and abort refused after a cutover timeout | F1-07, F1-03, F1-02 |
| RC11 | T2 | _part_name silently truncates to 63 bytes; obtain then skips every forward candidate by name and no forward grid is ever built | F1-05 |
| RC12 | T1 | Cutover replays row triggers from pg_get_triggerdef without tgenabled: DISABLED triggers fire, ALWAYS/REPLICA become ORIGIN | F1-08 |
| RC13 | T1 | Native bounds stored as timestamptz::text in the writing session's DateStyle: a DMY transmute and an MDY maintain disagree and retain drops live partitions | F10-07 |
| RC14 | T2 | archive_ledger keyed (parent_table, lo) and matched by child_name: regrain's swap leaves the source's rows; the documented rename procedure orphans them; archiving wedges on the primary key | F10-06, F10-08 |
| RC15 | T2 | Raw nspname cast with ::regnamespace re-parses it as an identifier: mixed-case schemas wedge archive and retire | F10-10 |
| RC16 | T2 | _next_archive_chunk's next distinct control value is decoded at the encoding's unit: a unit holding a chunk's worth of rows collapses to lo and archiving stops silently | F10-03 |
| RC17 | T2 | maintain does not re-apply lock_timeout after the retain COMMIT: auto-regrain's DETACH waits with no timeout and blocks the parent | F3-03 |
| RC18 | T3 | set_regrain(null) mid-flight keeps the cursor: the regrain is neither completed nor swept, capture and the TRUNCATE refusal stay installed | F3-05, F9-05 |
| RC19 | T2 | set_regrain's coarser-than-step guard compares widths at the anchor only: '30 days' on a monthly grid wedges auto-regrain on a February cell | F9-10 |
| RC20 | T3 | set_archive_fn never checks the strategy's return type; a mis-typed result is mapped onto covered_hi | F4-04 |
| RC21 | T3 | _enforce_write_blocks resets archive coverage by name before the identity refusal, destroying the real partition's ledger rows | F4-06 |
| RC22 | T3 | retire drops the attached coarse source of an in-flight regrain once archiving covers it, orphaning the fine copies | F10-09 |
| RC23 | T1 | pgpm_archive transports call pgpm._encode without partition_tz: a naive timestamp control on a non-UTC grid archives the wrong hour's rows | F6-02 |
| RC24 | T1 | pgpm_archive S3 key strips the sign from a negative lo: two partitions share one object key and the second upload overwrites the first | F6-03 |
| RC25 | T3 | archive.to_s3 ignores archive.config.compress; SigV4 x-amz-date is taken from now() so a long transaction gets RequestTimeTooSkewed | F6-04, F6-05 |
| RC26 | T4 | Docs: guide's TLE snippet pins 0.4.0; reference names a log action nothing writes (from_hypertable_adopt_fk) | F5-05, F5-06 |
| RC27 | T5 | Tests: throws_ok with a NULL pattern accepts the 2D000 a non-refusing committing procedure raises (tests/72, timescale 08/10/14); perf.yml filter omits wrapper-driven tests 102/103/104 | F7-02, F7-03, F7-04 |

27 root-cause groups over 49 verified findings (44 distinct defects; duplicates across finders: F2-04 = F9-08; F2-05 = F8-05; F2-06 = F8-04 = F9-09; F3-05 = F9-05). Groups by top tier: T1 7, T2 13, T3 5, T4 1, T5 1.

Reproductions rebuilt by the verifier because the finder's liveness or fixture checks failed on the pristine tree: F2-04, F10-02, F10-03, F10-05, F10-07 (part A); F6-02, F6-03, F6-04 needed tests/archive/fixtures.sql loaded (the coordinator added it to their install lists and re-ran them). Tiers changed by verifiers: F9-05 T4 to T3, F9-10 T3 to T2, F10-08 T4 to T2.

## Coordinator notes

### Model-tier split (same slice, same brief, two finders)

| slice | finder | model | claims | seed hits | candidates | verified findings | tokens | wall |
|---|---|---|---|---|---|---|---|---|
| S3 regrain | F3 | fable, high | 5 | 2 (S2, S8) | 3 | 3 (T2, T3, T3) | 248k | 23 min |
| S3 regrain | F9 | opus (override) | 10 | 3 (S2, S8, S5) | 7 | 7 (T1, T1, T2, T2, T2, T2, T3) | 269k | 26 min |
| S4 retain | F4 | fable, high | 6 | 2 (S1, S4) | 4 | 4 (T2, T3, T3, T3) | 237k | 24 min |
| S4 retain | F10 | opus (override) | 10 | 2 (S1, S4) | 8 | 8 (T1, T2 x6, T3) | 302k | 24 min |

Both pairs: the Opus finder produced more claims at the same precision (1.00), equal or better seed
recall, and found Tier 1 defects its counterpart did not (F9-03, F9-04, F10-07), for 8 to 27 percent
more tokens. The union of each pair also exceeded either member (F3-03 and F3-02 were F3's alone),
so part of the gain is a second independent draw rather than the model. Two slices is a small
sample; the direction is clear enough to make Opus the default finder model for pass 3 and to keep
duplicating two slices.

### What the verifiers changed

- Tiers: F9-05 T4 to T3, F9-10 T3 to T2, F10-08 T4 to T2.
- Reproductions rebuilt by the verifier because the finder's liveness or guard checks failed on the
  pristine tree (the classifier read those failures as "fails"): F2-04, F10-02, F10-03, F10-05,
  F10-07 (part A). The defects are confirmed; the finder's files are not yet the acceptance test and
  must be replaced by the verifier's before filing. The classifier now separates liveness failures
  from detected defects (PR #493).
- Duplicates (independent finders, same defect): F2-04 = F9-08; F2-05 = F8-05; F2-06 = F8-04 =
  F9-09; F3-05 = F9-05. 45 verified claims are 40 distinct defects.

### Hypotheses (not counted), from the finders' null-results files

F1 5, F2 5, F3 4, F4 5, F5 5, F6 7, F7 5, F8 6, F9 4, F10 6: 52 in all.

### Seeds

All nine hit (S6, the Parquet snapshot seed, by F6-01). Every seed was
hit by at least two finders except S7 (F2, F7) and S9 (F7 only). Attribution needed isolation for
every claim in regrain_step, where S2 and S8 sit 50 lines apart.

### Tooling defects found by running the pass

1. `plant_seeds.py` applied novel patches with `git apply --index` after unstaged mutation edits and
   failed on the first patch. Fixed before planting.
2. The classifier anchored `not ok` at column 0; psql indents result rows by one space, so 18
   reproductions that plainly failed were read as not_reproduced. Fixed, all runs redone.
3. Nearest-line seed attribution was wrong for every claim between two seeds 50 lines apart.
   Replaced by isolation (re-run against a single-seed tree).
4. The archive module's claims need the pgsql-http container; the classifier crashed on the install
   failure. Claims may now name a container and an install failure is a recorded outcome.
5. A failed liveness witness read as a detected defect (see above). Fixed.

### Environment caveats

- The pristine checkout was on the tooling-fix branch (581e87a plus commits touching only
  `scripts/review/`); every verifier confirmed `pgpm_core/`, `pgpm_archive/` and `docs/` identical to
  the pinned commit.
- Finders ran with the pristine checkout readable on the same machine; blindness to it was by
  instruction, not by construction. No finder's transcript or claims show it was read.
- No TimescaleDB container (ECR pull limits); S5 reviewed `pgpm_hypertable` statically.
- Finders' self-reported time ("about 2 hours") was wrong; measured wall-clock was 19 to 35 minutes
  each. Budget lines use measured figures.
- Concurrency cap of 20 subagents: the 45 verifiers ran in waves; verification of the first claims
  started while other finders were still hunting (a pipeline, not a barrier).

### Budget, measured

- Finders: 10 agents, 2,775,621 tokens, 19 to 35 minutes wall-clock each (F6 35 minutes, the SigV4
  reproduction included), all started 15:32 UTC.
- Verifiers: 49 agents, 1,887,258 tokens, mean 38,515, 1 to 16 minutes each.
- Coordinator and classifier runs not counted. Pass total 4,662,879 agent tokens; hunt start
  15:32 UTC, last verdict 17:00Z UTC.
- Cost per verified finding: 95,160 tokens; per distinct defect (44): 105,974; per Tier 1
  finding (7): 666,125.

### Issues filed

Issues #496 to #522, one per root-cause group, Tier 1 first (#496 to #502), each carrying the
verified reproductions as the acceptance test for its fix.
